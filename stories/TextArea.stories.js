import { createTextArea } from '../src/components/TextArea';

// ─────────────────────────────────────────────────────────────
// Docs 用コンポーネント説明
// Figmaデザインシステム "Design-System-2.0--AI-Native-" Text area ページより
// node-id: 219:3357
// ─────────────────────────────────────────────────────────────
const COMPONENT_DESCRIPTION = `
## 概念

どういうものか。何のためにあるのか。根源的で、絶対に外しては成り立たない本質。

> **ストレスなく長文を入力するための、十分な広さのあるエリア。**

---

## 基本設計

### 状態一覧

| 状態 | ボーダー | 背景 | テキスト色 | 説明 |
|------|----------|------|-----------|------|
| 未入力 | グレー 1px（\`#d6d9db\`） | 白 | プレースホルダー（\`#858c94\`） | 何も入力されていない初期状態 |
| 入力中 | 青 2px（\`#318bf7\`） | 白 | \`#222\` | フォーカス中・入力中の状態 |
| 入力済み | グレー 1px（\`#d6d9db\`） | 白 | \`#222\` | 入力が完了した状態 |
| Disabled | グレー 1px（\`#d6d9db\`） | グレー（\`#f7f7f8\`） | \`#adb2b8\` | 操作不可状態 |
| Error | 赤 1px（\`#eb4d38\`） | 薄赤（\`#fdf2f0\`） | \`#222\` | バリデーションエラー時。エラーメッセージを下部に表示 |

### サイズ仕様

| 項目 | 仕様 |
|------|------|
| デフォルト高さ | 200px |
| 最小高さ | 1行分（37px）まで縮小可能（必要な場合のみ） |
| 最大高さ | 画面ごとにデザイナーが指定する。超過時はスクロールを表示 |
| padding | 8px 12px |
| border-radius | 4px |
| フォント | Hiragino Sans W3 / 14px / line-height 1.5 |
| リサイズ | ユーザーが縦方向に拡張可能（右下のリサイズハンドル） |

---

## 使用時に気をつけておくこと

- 複数行入力可能なことが直感的に分かるサイズにする。
- 入力内容に応じて高さが拡張される、もしくはユーザーが自身で広げられるようにする。
- 入力されるテキストをあらかじめ想定して、デフォルトの高さを決めること。
- 文字数上限を決めておく。
- 文字数を超えた場合のエラー表現はプロダクト内で統一させておく。
- 何を入力するべきかがわかるラベルもセットで表示する（ラベルは別パーツ）
  - ラベルは項目名として常に表示、プレースホルダーは入力例や補足を示す用途とし、役割を分ける
`;

// ─────────────────────────────────────────────────────────────
// Meta
// ─────────────────────────────────────────────────────────────
export default {
  title: 'TextArea',
  tags: ['autodocs'],
  render: (args) => createTextArea(args),
  parameters: {
    layout: 'padded',
    docs: {
      description: {
        component: COMPONENT_DESCRIPTION,
      },
    },
  },
  argTypes: {
    state: {
      control: 'select',
      options: ['default', 'active', 'filled', 'disabled', 'error'],
      description: '表示状態（Figma の5状態に対応）',
      table: { defaultValue: { summary: 'default' } },
    },
    placeholder: {
      control: 'text',
      description: 'プレースホルダーテキスト',
      table: { defaultValue: { summary: '入力してください' } },
    },
    value: {
      control: 'text',
      description: '入力済みテキスト（filled / active / error 時に使用）',
      table: { defaultValue: { summary: '' } },
    },
    errorMessage: {
      control: 'text',
      description: 'エラーメッセージ（error 状態のときのみ表示）',
      table: { defaultValue: { summary: '必須項目です。' } },
    },
  },
  args: {
    state: 'default',
    placeholder: '入力してください',
    value: '',
    errorMessage: '必須項目です。',
  },
};

// ─────────────────────────────────────────────────────────────
// Default（未入力）
// ─────────────────────────────────────────────────────────────
/** 未入力状態。プレースホルダー（`#858c94`）を表示。ボーダーはグレー 1px。 */
export const Default = {};

// ─────────────────────────────────────────────────────────────
// Active（入力中）
// ─────────────────────────────────────────────────────────────
/**
 * 入力中（フォーカス）状態。ボーダーが青 2px（`#318bf7`）になる。
 * フォーカス時の視覚的フィードバック。
 */
export const Active = {
  parameters: {
    docs: {
      description: {
        story:
          '入力中（フォーカス）状態。ボーダーが青 2px（`#318bf7`）に変化する（Figma「入力中」）。',
      },
    },
  },
  args: {
    state: 'active',
    value: '入力してください',
  },
};

// ─────────────────────────────────────────────────────────────
// Filled（入力済み）
// ─────────────────────────────────────────────────────────────
/** 入力済み状態。テキスト色 `#222`、ボーダーはグレー 1px に戻る。 */
export const Filled = {
  parameters: {
    docs: {
      description: {
        story:
          '入力済み状態。テキストが `#222` で表示される（Figma「入力済み」）。',
      },
    },
  },
  args: {
    state: 'filled',
    value: '入力済みのテキストが表示されます。複数行にわたる長文も問題なく表示されます。',
  },
};

// ─────────────────────────────────────────────────────────────
// Disabled
// ─────────────────────────────────────────────────────────────
/** 操作不可状態。背景グレー（`#f7f7f8`）、テキスト・プレースホルダー色が `#adb2b8` に変化。 */
export const Disabled = {
  parameters: {
    docs: {
      description: {
        story:
          'Disabled 状態。背景 `#f7f7f8`、文字色 `#adb2b8`、リサイズ不可（Figma「Disabled」）。',
      },
    },
  },
  args: {
    state: 'disabled',
  },
};

// ─────────────────────────────────────────────────────────────
// Error
// ─────────────────────────────────────────────────────────────
/**
 * エラー状態。ボーダーが赤（`#eb4d38`）、背景が薄赤（`#fdf2f0`）になり、
 * エリア下部にエラーメッセージを表示する。
 */
export const Error = {
  parameters: {
    docs: {
      description: {
        story:
          'Error 状態。赤ボーダー（`#eb4d38`）＋薄赤背景（`#fdf2f0`）＋エラーメッセージを表示（Figma「Error」）。',
      },
    },
  },
  args: {
    state: 'error',
    value: '',
    errorMessage: '必須項目です。',
  },
};
